spring:
  application:
    name: SpringBootDatabaseDemo

  datasource:
    url: jdbc:postgresql://localhost:5432/employeemanagementdb
    username: VINAYAK.R
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # create-drop / update / create / validate / none
    show-sql: true